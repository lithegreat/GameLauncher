<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="GameLauncher.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GameLauncher"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="游戏启动器">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom Title Bar -->
        <Grid x:Name="CustomTitleBar" Grid.Row="0" Height="Auto">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="138"/><!-- Reserved space for Windows caption buttons -->
            </Grid.ColumnDefinitions>
            
            <!-- App Icon and Title -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                <TextBlock Text="🎮" FontSize="20" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock x:Name="TitleTextBlock" Text="游戏启动器" 
                           Style="{StaticResource CaptionTextBlockStyle}" VerticalAlignment="Center" Opacity="0.8"/>
            </StackPanel>

            <!-- Draggable area -->
            <Border Grid.Column="1" Background="Transparent" x:Name="DragArea"/>
            
            <!-- Reserved space for Windows caption buttons (minimize, maximize, close) -->
            <!-- Grid.Column="2" is intentionally left empty for system buttons -->
        </Grid>

        <!-- NavigationView -->
        <NavigationView x:Name="nvSample" 
                        Grid.Row="1" 
                        IsBackButtonVisible="Collapsed" 
                        PaneDisplayMode="Left"
                        IsSettingsVisible="False">
            <NavigationView.MenuItems>
                <NavigationViewItem Icon="Play" x:Name="MyGamesNavItem" Content="我的游戏" Tag="GamesPage" />
            </NavigationView.MenuItems>

            <NavigationView.FooterMenuItems>
                <!-- Add our custom settings item -->
                <NavigationViewItem Icon="Setting" x:Name="SettingsNavItem" Content="设置" Tag="SettingsPage" />
            </NavigationView.FooterMenuItems>

            <Frame x:Name="contentFrame"/>
        </NavigationView>
    </Grid>
</Window>
